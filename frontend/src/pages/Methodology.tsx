import { BookOpenText } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

export default function Methodology() {
  return (
    <PlaceholderPage
      title="Methodology"
      description="Base period, route basket, weights, price normalization, index formula and data-quality rules."
      checkpoint="Checkpoint C"
      icon={BookOpenText}
    />
  );
}
